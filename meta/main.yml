---
galaxy_info:
  author: Davide Panarese
  description: Playbook for icinga2-client.
  company: Enter srl
  license: MIT
  min_ansible_version: 2.4
  platforms:
    - name: Ubuntu
      versions:
        - xenial
  galaxy_tags:
    - molecule

dependencies:
  - {role: galaxy.ansible-icinga2-master, version: 1.1.1}
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
